            {% if ('edit' in actions) and (not hide_edit) %}

                <a href="{{ "{{ path('" ~ route_name_prefix ~ "_edit', { 'id': entity.id }) }}" }}" class="btn btn-info" role="button">
                    <span class="glyphicon glyphicon-edit"></span> Modificar</a>

            {% endif %}
            {% if ('delete' in actions) and (not hide_delete) %}

                <a href="{{ "{{ path('" ~ route_name_prefix ~ "_delete', { 'id': entity.id }) }}" }}" class="btn btn-danger" role="button">
                    <span class="glyphicon glyphicon-trash"></span> Eliminar</a>

            {% endif %}
            
                <a href="{{ "{{ path('" ~ route_name_prefix ~ "') }}" }}" class="btn btn-info" role="button">
                    <span class="glyphicon glyphicon-list"></span> Listado</a>
